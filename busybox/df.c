#include <stdio.h>
#include <stdlib.h>
#include <errno.h>

void print_help();

int main(int argc, char const *argv[])
{
    
    return EINPROGRESS;
}

void print_help() {
    fputs("usage: df [-h]\n\n", stderr);

    fputs("Report file system disk space usage\n", stderr);

    exit(EINVAL);
}